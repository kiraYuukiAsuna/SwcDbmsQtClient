#pragma once

#include <string>

namespace version {
    // 项目版本号
    static const std::string VERSION = "@PROJECT_VERSION@";
    
    // Git信息
    static const std::string GIT_COMMIT_HASH = "@GIT_COMMIT_HASH@";
    static const std::string GIT_BRANCH = "@GIT_BRANCH@";
    static const std::string GIT_COMMIT_DATE = "@GIT_COMMIT_DATE@";
    
    // 完整版本字符串
    static const std::string VERSION_STRING = 
        VERSION + " (commit: " + GIT_COMMIT_HASH + 
        ", branch: " + GIT_BRANCH + 
        ", date: " + GIT_COMMIT_DATE + ")";
}

